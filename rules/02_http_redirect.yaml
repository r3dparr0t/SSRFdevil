#2. http_redirect.yaml
meta:
  id: http_redirect
  version: 2
  name: "HTTP Redirect to Localhost"
  description: "Uses HTTP redirect to localhost via 301/302"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 55
  confidence: 90
  severity: medium
  tags: [redirect, localhost, bypass]
  references: []

match:
  schemes: [http, https]
  kinds: [Endpoint]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "http://localhost/redirect" }
    end