# 1. ipv6_loopback.yaml
meta:
  id: ipv6_loopback
  version: 2
  name: "IPv6 Loopback Bypass"
  description: "Uses IPv6 loopback address"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 70
  confidence: 95
  severity: medium
  tags: [ipv6, loopback, bypass]
  references: []

match:
  schemes: [http, https]
  kinds: [Endpoint, Document]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback, Webhook]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "http://[::1]/" }
    end