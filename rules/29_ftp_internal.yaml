# 29. ftp_internal.yaml
meta:
  id: ftp_internal
  version: 2
  name: "FTP Protocol Internal"
  description: "Uses ftp:// to access internal FTP"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 55
  confidence: 60
  severity: medium
  tags: [ftp, internal]
  references: []

match:
  schemes: [http, https, ftp]
  kinds: [Endpoint]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback, Webhook]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "ftp://127.0.0.1/" }
    end
