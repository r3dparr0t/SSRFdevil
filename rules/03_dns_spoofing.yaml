meta:
  id: dns_spoofing
  version: 1
  name: "DNS Spoofing to 127.0.0.1"
  description: "Uses domain that resolves to localhost"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-06-19"
  rank: 65
  confidence: 85
  severity: high
  tags: [dns, spoofing, localhost]
  references: []

match:
  schemes: [http, https]
  requires: [hostname]
  supports: [dns]

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return {
        url = "http://localhost.localdomain/"
      }
    end
