# 30. unknown_host_header.yaml
meta:
  id: unknown_host_header
  version: 2
  name: "Unknown Host Header Attack"
  description: "Sends request with Host header pointing to internal IP"
  author: "SSRFdevil"
  created: "2026-06-19"
  updated: "2026-07-21"
  rank: 85
  confidence: 80
  severity: high
  tags: [host_header, internal, bypass]
  references: []

match:
  schemes: [http, https]
  kinds: [Endpoint]
  allowed_methods: [GET, POST]
  required_tags: [Redirect, Callback, Webhook]
  require_params: true

script:
  language: lua
  entry: bypass
  source: |
    function bypass(ctx)
      return { url = "http://example.com", headers = { ["Host"] = "127.0.0.1" } }
    end
